# socket.io (websockets) feature interface module
#

name: websocket
description: socket.io interface

run: !coffee/function |
  (model, runtime) ->
    source = model.parent
    io = source.require 'socket.io'
    console.info "websocket: binding forgery to /socket.io".grey
    return (io runtime.express?.server)
