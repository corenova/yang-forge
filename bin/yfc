#!/usr/bin/env node

var url  = require('url');
var core = __dirname + '../core.yaml';

require('..')
  .load(url.parse("file:#{core}"))
  .then( function (res) {
    res.create('forge').run('cli');
  });
